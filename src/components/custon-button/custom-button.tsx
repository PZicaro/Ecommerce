import { CustomButtonContainer } from './custom-button.styles'
import React, { FunctionComponent } from 'react'

const CustomButton: FunctionComponent = ({ children }) => {
  return (
    <CustomButtonContainer >
        {
    }
  </CustomButtonContainer>
  )
}

export default CustomButton
